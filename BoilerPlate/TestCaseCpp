#include <bits/stdc++.h>
using namespace std;
ios::sync_with_stdio(0);
cin.tie(0);

typedef long long ll;
typedef vector<int> vi;
typedef pair<int,int> pi;
#define F first
#define S second
#define PB push_back
#define MP make_pair
#define REP(i,a,b) for (int i = a; i <= b; i++)

int main(){
    int t;
    cin>>t;
    while(t>0){
        t--;
        
    }
    return 0;
}

// string s;
// getline(cin, s);

//for file input output
// freopen("input.txt", "r", stdin);
// freopen("output.txt", "w", stdout);

// __int128_t -> //128 bit Integer

// printf("%.9f\n", x); //print floating points with precision till 9


// comparing float/double
// if(abs(a-b)<1e-9){
//     //both are equal
// }


//vector operations ex
// v.push_back(make_pair(y1,x1));
// v.push_back(make_pair(y2,x2));
// int d = v[i].first+v[i].second;

// v.PB(MP(y1,x1));
// v.PB(MP(y2,x2));
// int d = v[i].F+v[i].S;